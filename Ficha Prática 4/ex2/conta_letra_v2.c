#include <string.h>
#include <stdio.h>

#include "args.h"

int conta_letra(char *str, char *c)
{
    int max = strlen(str);
    int ocurrence_count = 0;

    for(int i = 0; i < max; i++)
    {
    }

    return ocurrence_count;
}